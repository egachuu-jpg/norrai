# data plane (Norr AI box / n8n)
COS_DB_URL=postgresql://cos_api:...@localhost:5432/norrai
COS_API_TOKEN=            # generate: openssl rand -hex 32
ANTHROPIC_API_KEY=        # n8n credential store preferred over env

# conversational plane (Hermes VPS) — ONLY these three, per PRD §6.1
# ANTHROPIC_API_KEY=
# TELEGRAM_BOT_TOKEN=
# COS_API_TOKEN=
