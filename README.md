# norrai

## Workflow

**Always branch from main before making changes.**

```bash
git checkout main
git pull
git checkout -b your-change-name
```

Make your changes, commit, and merge back into main. After merging, pull latest:

```bash
git checkout main
git pull
```